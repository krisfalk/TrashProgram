@model IEnumerable<MunicipalTrashProgram.Models.Worker>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WorkingZipCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WorkingZipCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Worker_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Worker_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Worker_id })
        </td>
    </tr>
}

</table>
